<?php
/**
 * Exemple de shortcode
 *
 * @package WordPress
 * @subpackage electro
 * @since electro 1.0
 * @author : Pascal RAMANANJARASOA
 */
add_shortcode('document', 'electro_render_document_shortcode');
function electro_render_document_shortcode($attr){
  $list_id = explode(',', $attr['id']);
  ob_start();
  ?>

     <!-- do html here -->

    <?php

  $content = ob_get_contents();
  ob_clean();

  return $content;
}

